---
'stl.quest': patch
---

Repaint a print's thumbnail, viewer, and download link after its model is replaced, instead of serving the browser's year-old cached copy of the file that was swapped out.
