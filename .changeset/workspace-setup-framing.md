---
'stlquest': patch
---

Set up a new workspace through its own two-step flow named after that workspace, so that adding a workspace no longer looks like resuming the account setup you already finished.
