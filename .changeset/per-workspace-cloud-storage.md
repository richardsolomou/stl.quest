---
'stlquest': minor
---

Connect your own Dropbox, Google Drive, or OneDrive account to a workspace once a super admin has registered the provider's app in Integrations, so that models land in storage the workspace owner controls instead of a single account shared across the deployment. Any connection already in use moves to the workspace using it.
