---
'stl.quest': patch
---

Show a clear terminal message instead of a retry that cannot work when the viewer cannot read a 3MF or STL file, and keep the real parse failure so unsupported 3MF features are diagnosable.
