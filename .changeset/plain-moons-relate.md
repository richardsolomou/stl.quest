---
'stl.quest': patch
---

Show initials in the requester's own colour instead of a Gravatar identicon when someone has no Gravatar, so a board of members reads as one interface rather than a row of unrelated generated images.
