---
'stlquest': patch
---

Set up every workspace through the same two steps named after that workspace, instead of a new account landing on "step 3 of 4" of a sequence that counted screens it had already passed.
