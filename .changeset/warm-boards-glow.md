---
'printhub': minor
---

Redesign the app around a shop-floor visual identity — request cards render as job tickets, board columns as stations, so the queue reads like a print farm's paper trail instead of a generic kanban board.
