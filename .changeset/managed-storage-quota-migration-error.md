---
'stlquest': patch
---

Tell operators that a storage migration onto managed hosted storage failed because it would exceed the storage quota, so that they know to free up space or raise the quota and retry.
