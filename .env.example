# Host paths for the two container mounts. Everything else — storage,
# authentication, telemetry — is configured in the app under Settings.
DATA_HOST_DIR=/mnt/HDDs/STL/.printhub-data
PRINTS_HOST_DIR=/mnt/HDDs/STL
